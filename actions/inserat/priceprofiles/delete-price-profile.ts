export const deletePriceprofile = async (values) => {
    try {

        const { title, description, price, freemiles, extraCost,
            profileId, token
            } = values;

    } catch(e : any)  {
        console.log(e);
        return;
    }
}